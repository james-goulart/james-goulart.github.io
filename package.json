{
  "name": "james-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "James Goulart — conversational portfolio with JamesCVBot",
  "scripts": {
    "start": "node scripts/sync-assets.js && node server.js",
    "dev": "node scripts/sync-assets.js && node server.js",
    "worker:dev": "wrangler dev",
    "sync:assets": "node scripts/sync-assets.js",
    "worker:deploy": "node scripts/sync-assets.js && wrangler deploy"
  },
  "dependencies": {
    "dotenv": "^17.4.0",
    "express": "^4.21.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "wrangler": "^4.81.0"
  }
}
